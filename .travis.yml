branches:
  only:
    - develop

sudo: required
dist: trusty

language: cpp

addons: 
  apt: 
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-5
      - libboost-all-dev

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_CXX_COMPILER=g++-5 -DPYBIND11_PYTHON_VERSION=2.7 ..
  - make cirkit
